# PR #53961 — fix(delivery): track and log silent delivery failures

**Worktree:** `~/openclaw-pr-53961`
**Branch:** `fix/delivery-status-tracking`
**Closes:** #53961 (the issue)
**Status:** draft, `proof: supplied` — #57843 soft-blocker dropped (closed by maintainer). Fits current maintainer appetite: bug fix, ~5 files, ~391 lines (under hold threshold). Un-draft after main stabilizes post-2026.5.9.

## Stated purpose

Make the silent-delivery-failure class of bug observable — when an agent's
reply makes it into the session transcript but never reaches the external
channel (e.g. stale channel context after a model fallback), operators
should learn about it via stderr, and internal callers should see it via
the function's return value.

## In scope (after slim — 2026-05-08)

- `deliveryStatus` object on `deliverAgentCommandResult`'s **return value**
  with fields: `requested`, `attempted`, `succeeded`, `hadPartialFailure?`,
  `error?`. So internal callers (gateway send handler, agent dispatcher)
  can branch on it.
- Stderr warning `[delivery] delivery requested but not completed: <reason>`
  with reason chain: no channel / internal channel / no target / threw /
  zero results. Fires only when delivery requested + payloads non-empty +
  not succeeded + not in JSON mode.
- `try/catch` wrapping `deliverOutboundPayloads` so non-bestEffort throws
  don't escape uncaught; bestEffort errors → `error: true` in status.
- Status-tracking unit tests in `src/agents/command/delivery.test.ts`.
- CHANGELOG entry under `## Unreleased > Changes` (added 2026-05-08; required by CONTRIBUTING).

## NOT in scope (slimmed out — moved to #57755)

- `deliveryStatus` field in the `--json` output envelope.
- Deferred-emission behavior (`emitJsonEnvelope` helper).
- `succeeded: boolean | "partial"` 3-state.
- No-payload case emitting `deliveryStatus` in JSON envelope.
- Emit-on-strict-throw before re-throwing.

These all belong to #57755 (the JSON-side delta).

## NOT in scope (separate concern)

- Hook-cancellation false-positive suppression — the `allCancelledByHook`
  approach from #57843 is moot now that #57843 is closed. The `// Note:`
  comment in code remains; revisit once the maintainer's new plugin-callback
  API expansion lands and exposes equivalent metadata.

## Relationships

- **No longer blocked by anything** — #57843 closed; hook-cancel
  false-positive noise is accepted until maintainer's new API lands.
- **Hard prerequisite for #57755** — #57755 surfaces *this* PR's
  `deliveryStatus` field in the `--json` envelope, so #57755 must land
  AFTER #53961 (or contain #53961's status tracking until merge).

## Sequencing

1. ~~#57843 first~~ — closed by maintainer.
2. **#53961 next** (this PR — clean bug fix, fits current maintainer appetite).
3. #57755 after — on hold pending refactor; will un-draft once main settles.

## Implementation notes / gotchas

- Upstream's `deliverAgentCommandResult` uses `mergeResultMetaOverrides`
  and `writeRuntimeJson` — preserve those calls verbatim during conflict
  resolution. The earlier rebase that bled JSON-side surface in here
  happened because I copied the old PR's full structure including its
  `runtime.log(JSON.stringify(...))` pattern instead of upstream's
  `writeRuntimeJson(runtime, ...)`. **Pattern:** when resolving conflicts
  on upstream-refactored helpers, preserve upstream's helper call sites;
  only add/remove the parts your PR is intended to change.
- Tests removed during slim (now belong to #57755): "returns
  deliveryStatus on early return when deliver=true but no payloads",
  "includes deliveryStatus in JSON output when deliver=true", "omits
  deliveryStatus from JSON output when deliver=false".

## Pending housekeeping (do on next touch)

- Drop the stale stash on this worktree: `cd ~/openclaw-pr-53961 && git stash drop`
  It's `stash@{0}: pre-rebase-2026-05-08` — a prior-session hook-cancellation
  experiment, superseded by the deferred patch in
  `~/my-openclaw-patches/deferred/2026-05-08-pr57755-hook-cancellation-followup/`.
  Not blocking.

## Lessons captured

- Bled #57755's surface into here during rebase because I didn't have
  written-down boundaries for what each PR should own. Wrote this doc to
  prevent recurrence.
